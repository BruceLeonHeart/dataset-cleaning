在命名不规范的情况下，重新调整文件名

usage: python rename.py --img_dir /img_dir --xml_dir /xml_dir --dest_dir /dest_dir
-----------------------------------------------------------------------------------------
jpg_dir:图片存放位置
xml_dir:VOC格式的xml文件存放位置
dest_dir:合并文件后所在的位置